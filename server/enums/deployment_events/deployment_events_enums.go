package deployment_events_enums

const (
	INITIAL_DEPLPYMENT string = "initial_deployment"
	RESTARTED          string = "restarted"
	NEW_DEPLOYMENT     string = "new_deployment"
	STOPPED            string = "stopped"
)
